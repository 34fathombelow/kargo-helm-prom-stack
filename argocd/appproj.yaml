apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: kargo-addons
  namespace: argocd
spec:
  clusterResourceWhitelist:
  - group: "*"
    kind:  '*'
  destinations:
  - name: '*'
    namespace: '*'
    server: '*'
  namespaceResourceWhitelist:
  - group: '*'
    kind: '*'
  sourceRepos:
  - '*'
  roles:
    - description: Role used for AppSet generation
      name: appset-generate
      policies:
        - p, proj:kargo-addons:appset-generate, applications, get, kargo-addons/*, allow
        - p, proj:kargo-addons:appset-generate, applicationsets, *, kargo-addons/*, allow
        - p, proj:kargo-addons:appset-generate, clusters, get, kargo-addons/*, allow
        - p, proj:kargo-addons:appset-generate, repositories, get, kargo-addons/*, allow
