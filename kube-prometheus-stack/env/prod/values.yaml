kube-prometheus-stack:
  alertmanager:
    alertmanagerSpec:
      image:
        tag: v0.32.0
    ingress:
      enabled: true
      hosts:
        - alertmanager-prod.localhost
      paths:
        - /
  grafana:
    image:
      tag: 13.1.0-24753821026-ubuntu
    adminPassword: admin
    sidecar:
      dashboards:
        enabled: true
        labelValue: ""
    additionalDataSources:
     - name: prometheus-local
       type: prometheus
       uid: prometheus-local
       url: http://host.docker.internal:9090
       access: proxy
       isDefault: false
       jsonData:
         httpMethod: POST
         timeInterval: 15s
    ingress:
      enabled: true
      hosts:
        - grafana-prod.localhost
      paths:
        - /
  prometheus:
    prometheusSpec:
      image:
        tag: v3.11.2
    ingress:
      enabled: true
      hosts:
        - prometheus-prod.localhost
      paths:
        - /
  prometheus-node-exporter:
    prometheus:
      monitor:
        interval: 15s
